# MaxContact Skills (placeholder)

Intended areas, per the build spec — not filled with generic textbook content, only added when there's real reusable operational knowledge to capture:

- `kubernetes/` — MaxContact-specific K8s conventions (not generic K8s docs)
- `terraform/` — module conventions, provider setup specific to MaxContact's cloud accounts
- `security-review/` — what "secure" means for MaxContact specifically (data classification, compliance constraints)
- `incident-response/` — MaxContact's actual incident process, escalation paths
- `ci-cd/` — the org's actual GitHub Actions conventions, required checks
- `platform-architecture/` — Crucible-specific and other real platform conventions

A skill goes here because it captures something a generic Kubernetes/Terraform/security skill wouldn't know — MaxContact's own conventions, gotchas, and decisions. Add via `SKILL.md` per directory (see pi's Agent Skills format).
